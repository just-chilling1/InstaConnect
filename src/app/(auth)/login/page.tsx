import AuthForm from "@/components/AuthForm";

export default function LoginPage() {
  return (
    <AuthForm
      heading="Welcome back"
      subheading="Sign in with a one-time code sent to your email."
    />
  );
}
